# Render.com blueprint – free tier
services:
  - type: worker
    name: clipcash-worker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    plan: free
